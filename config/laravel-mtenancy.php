<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Default tenant migrations path
    |--------------------------------------------------------------------------
    |
    | Path for each tenant migration (not the global tenant router), here is
    | where you should put your custom app migrations and will be populated
    | to each created tenant.
    |
    */

    'migrations_path' => 'database/migrations/tenants'
];
